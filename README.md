# open-gdocs
A simple Windows script that lets you open Google Docs, Sheets, Slides and other Google Drive shortcut files (`.gdoc`, `.gsheet`, `.gslides`, `.gform`, `.gdraw`, `.gscript`, `.gjam`, `.gtable`) **directly from File Explorer** — without installing Google Drive for Desktop.
